// Centraliza os nomes das rotas nomeadas do aplicativo, organizando a
// navegação em um único ponto (evita strings soltas espalhadas pelo código).
class AppRoutes {
  AppRoutes._();

  static const String home = '/';
  static const String asteroidDetail = '/asteroid-detail';
}
